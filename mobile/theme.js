import { backgroundColour, primaryColour, secondaryColour, textColour } from './src/app/styles/styles';

const theme = {
    dark: false,
    roundness: 4,
    colors: {
        primary: primaryColour,
        accent: secondaryColour,
        background: backgroundColour,
        text: textColour,
    },
};

export default theme;